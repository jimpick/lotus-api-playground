function start (cb) {
  // const ws = new WebSocket(`ws://${location.host}/api/rpc/v0`)
  console.log('Jim1')
  /*
  ws.onopen = function (event) {
    console.log('Jim2')
    cb(ws)
  }
  */
}

window.ws = {
  start
}